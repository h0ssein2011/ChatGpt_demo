# ChatGpt_demo

This repository contains a simple Snake game implemented with Python and Pygame.

## Requirements
- Python 3
- Pygame library (install with `pip install pygame`)

## Running the game
Execute the following command:

```bash
python3 snake_game.py
```

Use the arrow keys to control the snake. Eat the red food squares to grow. If the snake runs into itself, the game ends.

